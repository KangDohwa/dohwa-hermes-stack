# F3 comment-mode smoke canary

This documentation-only change verifies the versioned reviewer deployment in
`comment` mode. It must produce a review and Discord report without changing
the pull request to Draft or attempting a merge.
